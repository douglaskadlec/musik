export const progress = document.querySelector('.progress') as HTMLElement
export const bar = document.querySelector('.bar') as HTMLElement
export const image = document.querySelector('.image') as HTMLImageElement
export const title = document.querySelector('.title') as HTMLElement
export const buttons = Array.from(document.querySelectorAll<HTMLButtonElement>('.playlist button'))